FluxVault sparse-package visual assets are generated or supplied by the release
packaging lane. Developer packages copy this folder so the sparse package shape
is explicit even before production signing assets are final.
